# 医疗病历数据生成原理详解

## 📚 目录
1. [核心生成方法](#核心生成方法)
2. [模板驱动生成](#模板驱动生成)
3. [规则引擎](#规则引擎)
4. [随机化策略](#随机化策略)
5. [数据一致性保证](#数据一致性保证)

---

## 1. 核心生成方法

### 🎯 总体架构

```
┌─────────────────────────────────────────────────────┐
│           数据生成流程 (每个患者)                      │
└─────────────────────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤1: 随机选择疾病类型         │
        │  10种疾病 → 随机选1种            │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤2: 生成基本信息             │
        │  姓名、性别、年龄、联系方式      │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤3: 基于疾病生成主诉         │
        │  从症状模板库中选择              │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤4: 生成现病史              │
        │  拼接模板，描述病情发展          │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤5: 随机选择既往史/个人史    │
        │  从预定义模板库中抽取            │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤6: 根据疾病调整生命体征     │
        │  高血压→血压↑, 糖尿病→血糖↑     │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤7: 生成检验结果             │
        │  血常规、生化、心电图            │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤8: 设置治疗方案             │
        │  疾病特异性药物和指导            │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤9: 生成5天病程记录          │
        │  模拟病情演变                    │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  步骤10: 生成3天护理记录         │
        │  每天3次记录                     │
        └─────────────────────────────────┘
```

---

## 2. 模板驱动生成

### 📋 模板库结构

生成系统使用**预定义模板库** + **随机组合**的方法：

#### 2.1 疾病模板库

```python
DISEASES = [
    "2型糖尿病",           # 10种常见疾病
    "高血压",
    "冠心病",
    "慢性阻塞性肺疾病",
    "胃炎",
    "脂肪肝",
    "甲状腺功能减退",
    "骨关节炎",
    "慢性肾病",
    "支气管哮喘"
]

# 随机选择: disease = random.choice(DISEASES)
```

#### 2.2 症状模板库（疾病特异性）

```python
SYMPTOMS = {
    "2型糖尿病": [
        "口渴、多饮、多尿",    # 典型症状1
        "乏力、体重下降",      # 典型症状2
        "视物模糊"             # 典型症状3
    ],
    "高血压": [
        "头晕、头痛",
        "心悸",
        "胸闷"
    ],
    "冠心病": [
        "胸闷、胸痛",
        "气短",
        "心悸、乏力"
    ],
    # ... 其他疾病
}

# 使用方法:
# symptom = random.choice(SYMPTOMS[disease])
```

#### 2.3 病史模板库（通用）

```python
# 既往史模板 (5种)
PAST_HISTORY = [
    "既往体健",
    "高血压病史5年",
    "糖尿病病史3年",
    "冠心病病史2年",
    "否认肝炎、结核病史"
]

# 个人史模板 (20种)
PERSONAL_HISTORY = [
    "无吸烟史，偶尔饮酒",
    "吸烟史20年，每日1包",
    "否认吸烟、饮酒史",
    "饮酒史10年，每日50ml白酒"
    # ... 更多模板
]

# 家族史模板 (10种)
FAMILY_HISTORY = [
    "父母健在，体健",
    "父亲有高血压病史",
    "母亲有糖尿病病史",
    "否认家族遗传性疾病史",
    "家族中有心脏病史"
]

# 过敏史模板 (8种)
ALLERGY_HISTORY = [
    "无药物过敏史",
    "青霉素过敏",
    "磺胺类药物过敏",
    "海鲜过敏"
]
```

#### 2.4 体格检查模板

```python
PHYSICAL_EXAM_TEMPLATE = """
一般情况: {general_condition}
生命体征: T {temp}℃, P {pulse}次/分, R {resp}次/分, BP {bp_sys}/{bp_dia} mmHg
心脏: 心率{hr}次/分，律齐，心音有力，各瓣膜听诊区未闻及病理性杂音
肺部: 双肺呼吸音清，未闻及干湿性啰音
腹部: 腹软，无压痛、反跳痛，肝脾未触及
四肢: 双下肢无水肿
神经系统: 神志清楚，言语流利，四肢活动自如
"""

# 使用模板填充变量生成最终文本
```

---

## 3. 规则引擎

### ⚙️ 疾病特异性规则

#### 规则1: 生命体征调整

```python
def generate_vital_signs(disease):
    """根据疾病调整生命体征"""

    base_bp_sys = 120  # 正常收缩压
    base_bp_dia = 80   # 正常舒张压

    # 规则: 高血压患者 → 血压升高
    if disease == "高血压":
        base_bp_sys = random.randint(140, 160)  # ↑
        base_bp_dia = random.randint(90, 100)   # ↑

    # 规则: 糖尿病患者 → 血压偏高
    elif disease == "2型糖尿病":
        base_bp_sys = random.randint(125, 145)
        base_bp_dia = random.randint(80, 95)

    return {
        "体温": round(random.uniform(36.2, 37.2), 1),
        "脉搏": random.randint(60, 100),
        "呼吸": random.randint(16, 22),
        "收缩压": base_bp_sys,
        "舒张压": base_bp_dia
    }
```

**示例输出**:
- 正常患者: BP 120/80 mmHg
- 高血压患者: BP 152/95 mmHg ✓
- 糖尿病患者: BP 135/88 mmHg ✓

#### 规则2: 检验结果调整

```python
def generate_lab_results(disease):
    """根据疾病调整化验指标"""

    # 规则: 糖尿病患者 → 血糖升高
    if disease == "2型糖尿病":
        glu = round(random.uniform(8.0, 15.0), 1)  # 高血糖
    else:
        glu = round(random.uniform(4.5, 6.5), 1)   # 正常血糖

    results = {
        "空腹血糖": glu,
        "总胆固醇": round(random.uniform(3.5, 6.5), 2),
        "甘油三酯": round(random.uniform(0.8, 2.5), 2),
        # ...
    }

    # 规则: 心血管疾病 → 心电图异常
    if disease in ["高血压", "冠心病"]:
        results["心电图"] = random.choice([
            "ST-T改变",
            "左心室高电压"
        ])
    else:
        results["心电图"] = "正常心电图"

    return results
```

**示例输出**:
- 正常患者: 血糖 5.2 mmol/L, 心电图正常
- 糖尿病患者: 血糖 11.3 mmol/L ✓
- 高血压患者: 血糖 5.8 mmol/L, 心电图 ST-T改变 ✓

#### 规则3: 治疗方案映射

```python
def generate_treatment_plan(disease):
    """疾病 → 特异性治疗方案"""

    plans = {
        "2型糖尿病": [
            "1. 饮食控制，低糖低脂饮食",
            "2. 口服降糖药物: 二甲双胍 500mg 每日3次",
            "3. 监测血糖变化",
            "4. 定期复查糖化血红蛋白",
            "5. 加强运动，控制体重"
        ],
        "高血压": [
            "1. 低盐低脂饮食",
            "2. 口服降压药: 硝苯地平缓释片 30mg 每日1次",
            "3. 监测血压变化",
            "4. 定期复查",
            "5. 避免情绪激动，适量运动"
        ],
        "冠心病": [
            "1. 绝对卧床休息",
            "2. 抗血小板: 阿司匹林 100mg 每日1次",
            "3. 调脂: 阿托伐他汀 20mg 每晚1次",
            "4. 改善心肌供血: 单硝酸异山梨酯",
            "5. 心电监护"
        ],
        # ... 其他疾病的治疗方案
    }

    return "\n".join(plans.get(disease, ["对症治疗", "定期复查"]))
```

#### 规则4: 次要诊断关联

```python
def generate_diagnosis(disease):
    """主诊断 → 可能的次要诊断"""

    primary = disease
    secondary = []

    # 规则: 糖尿病常合并高血压和脂肪肝
    if disease == "2型糖尿病":
        if random.random() > 0.5:  # 50%概率
            secondary.append("高血压病")
        if random.random() > 0.7:  # 30%概率
            secondary.append("脂肪肝")

    # 规则: 高血压常合并糖尿病
    elif disease == "高血压":
        if random.random() > 0.6:  # 40%概率
            secondary.append("2型糖尿病")

    return {
        "主要诊断": primary,
        "次要诊断": secondary if secondary else ["无"]
    }
```

---

## 4. 随机化策略

### 🎲 多层次随机化

#### 层次1: 顶层随机（疾病选择）

```python
# 每个患者随机选择一种疾病
disease = random.choice(DISEASES)

# 结果: 疾病分布接近均匀
# 高血压: 14例
# 甲状腺功能减退: 14例
# 脂肪肝: 11例
# ...
```

#### 层次2: 人口学随机

```python
def generate_basic_info(index):
    """随机生成基本信息"""

    # 性别随机
    gender = random.choice(["男", "女"])

    # 年龄随机 (25-85岁)
    age = random.randint(25, 85)

    # 姓名随机组合
    surnames = ["王", "李", "张", "刘", "陈", ...]  # 10个姓氏
    given_names_male = ["伟", "强", "明", ...]      # 10个男名
    given_names_female = ["芳", "娟", "敏", ...]    # 10个女名

    surname = random.choice(surnames)
    if gender == "男":
        given_name = random.choice(given_names_male) + \
                     random.choice(given_names_male)
    else:
        given_name = random.choice(given_names_female) + \
                     random.choice(given_names_female)

    name = surname + given_name

    # 可能的姓名组合数:
    # 男性: 10 × 10 × 10 = 1,000种
    # 女性: 10 × 10 × 10 = 1,000种
    # 总计: 2,000种不同姓名
```

#### 层次3: 身份证号随机

```python
# 生成符合规则的18位身份证号
id_card = f"330101"  # 地区代码（杭州上城区）
        + f"19{1940 + age - 1:02d}"  # 出生年份
        + f"{random.randint(1,12):02d}"  # 月份
        + f"{random.randint(1,28):02d}"  # 日期
        + f"{random.randint(1000,9999)}"  # 顺序码

# 示例: 330101199201155678
```

#### 层次4: 症状时长随机

```python
def generate_chief_complaint(disease):
    """主诉 = 症状 + 随机时长"""

    duration = random.choice([
        "3天", "1周", "2周", "1个月", "3个月", "半年"
    ])

    symptoms = SYMPTOMS.get(disease, ["不适"])
    symptom = random.choice(symptoms)

    return f"{symptom} {duration}"

# 可能的组合:
# 糖尿病: 3种症状 × 6种时长 = 18种主诉
# 高血压: 3种症状 × 6种时长 = 18种主诉
# ...
```

#### 层次5: 现病史文本随机

```python
def generate_present_illness(disease, chief_complaint):
    """从多个模板中随机选择"""

    templates = [
        # 模板1: 缓慢起病型
        f"患者{chief_complaint}前无明显诱因出现{症状}，"
        f"逐渐加重，伴{伴随症状}。"
        f"曾自服{药物}，效果不佳...",

        # 模板2: 诱因明确型
        f"患者于{chief_complaint}前因{诱因}后出现{症状}，"
        f"呈{发作性质}发作，{是否伴随其他症状}...",

        # ... 更多模板
    ]

    return random.choice(templates)  # 随机选一个模板
```

#### 层次6: 数值随机（连续变量）

```python
# 体温: 36.2-37.2°C之间的随机浮点数
temp = round(random.uniform(36.2, 37.2), 1)
# 可能结果: 36.5, 36.8, 37.1, ...

# 脉搏: 60-100次/分之间的随机整数
pulse = random.randint(60, 100)
# 可能结果: 72, 85, 94, ...

# 血糖: 根据疾病的范围内随机
if disease == "2型糖尿病":
    glu = round(random.uniform(8.0, 15.0), 1)
else:
    glu = round(random.uniform(4.5, 6.5), 1)
```

---

## 5. 数据一致性保证

### 🔗 一致性链条

#### 链条1: 疾病 → 主诉 → 现病史

```python
# 保证逻辑一致性
disease = "2型糖尿病"
    ↓
chief_complaint = "口渴、多饮、多尿 3个月"  # 从糖尿病症状库选择
    ↓
present_illness = "患者口渴、多饮、多尿 3个月前..."  # 引用主诉
```

#### 链条2: 疾病 → 检验结果 → 诊断

```python
disease = "2型糖尿病"
    ↓
lab_results = {
    "空腹血糖": 11.3  # 高于正常（糖尿病特征）
}
    ↓
diagnosis = {
    "主要诊断": "2型糖尿病",  # 与疾病一致
    "次要诊断": ["高血压病"]  # 可能的合并症
}
```

#### 链条3: 诊断 → 治疗方案

```python
diagnosis = "高血压"
    ↓
treatment_plan = """
1. 低盐低脂饮食
2. 口服降压药: 硝苯地平缓释片
3. 监测血压变化
"""  # 高血压的标准治疗方案
```

#### 链条4: 病情 → 病程记录演变

```python
def generate_progress_notes(disease):
    """5天病程记录，模拟病情好转"""

    notes = []
    statuses = [
        "病情平稳",          # 第1天
        "症状较前减轻",      # 第2天
        "症状明显好转",      # 第3天
        "病情稳定",          # 第4天
        "症状基本缓解"       # 第5天
    ]

    for day, status in enumerate(statuses, 1):
        note = f"患者入院第{day}天，{status}。"
        notes.append(note)

    return notes
```

### ✓ 一致性验证示例

```
患者: P2025000003 张鹏杰

✓ 一致性检查:
  主诉: "视物模糊 3个月"
    ↓ 符合
  诊断: "2型糖尿病"（糖尿病可导致视物模糊）
    ↓ 符合
  血糖: 10.1 mmol/L（升高）
    ↓ 符合
  治疗: "口服降糖药物: 二甲双胍"
    ↓ 符合
  病程: "血糖较前下降，症状好转"

✓ 所有链条逻辑自洽！
```

---

## 6. 代码实现示例

### 完整的患者生成流程

```python
def generate_complete_medical_record(index):
    """生成一份完整病历"""

    # 步骤1: 随机选择疾病
    disease = random.choice(DISEASES)

    # 步骤2: 生成基本信息
    basic_info = generate_basic_info(index)

    # 步骤3: 生成主诉（依赖疾病）
    chief_complaint = generate_chief_complaint(disease)

    # 步骤4: 生成现病史（依赖疾病和主诉）
    present_illness = generate_present_illness(disease, chief_complaint)

    # 步骤5: 随机选择病史
    past_history = random.choice(PAST_HISTORY)
    personal_history = random.choice(PERSONAL_HISTORY)
    family_history = random.choice(FAMILY_HISTORY)
    allergy_history = random.choice(ALLERGY_HISTORY)

    # 步骤6: 生成生命体征（依赖疾病）
    vital_signs = generate_vital_signs(disease)

    # 步骤7: 生成体格检查（依赖生命体征）
    physical_exam = PHYSICAL_EXAM_TEMPLATE.format(
        general_condition="神志清楚，精神差",
        temp=vital_signs["体温"],
        pulse=vital_signs["脉搏"],
        resp=vital_signs["呼吸"],
        bp_sys=vital_signs["收缩压"],
        bp_dia=vital_signs["舒张压"],
        hr=vital_signs["心率"]
    )

    # 步骤8: 生成检验结果（依赖疾病）
    lab_results = generate_lab_results(disease)

    # 步骤9: 生成诊断（依赖疾病）
    diagnosis = generate_diagnosis(disease)

    # 步骤10: 生成治疗方案（依赖诊断）
    treatment_plan = generate_treatment_plan(disease)

    # 步骤11: 生成病程记录（5天）
    progress_notes = []
    for day in range(1, 6):
        note = generate_progress_note(disease, day)
        progress_notes.append(note)

    # 步骤12: 生成护理记录（3天×3次）
    nursing_records = []
    for day in range(1, 4):
        for time in ["08:00", "14:00", "20:00"]:
            record = generate_nursing_record(day, time)
            nursing_records.append(record)

    # 组装完整病历
    medical_record = {
        **basic_info,
        "入院日期": datetime.now().strftime("%Y-%m-%d %H:%M"),
        "主诉": chief_complaint,
        "现病史": present_illness,
        "既往史": past_history,
        "个人史": personal_history,
        "家族史": family_history,
        "过敏史": allergy_history,
        "生命体征": vital_signs,
        "体格检查": physical_exam,
        "实验室检查": lab_results,
        "诊断": diagnosis,
        "治疗方案": treatment_plan,
        "病程记录": progress_notes,
        "护理记录": nursing_records
    }

    return medical_record
```

---

## 7. 生成质量保证

### ✓ 质量控制措施

#### 1. 真实性检查
- ✅ 所有数值在医学合理范围内
- ✅ 症状与疾病匹配
- ✅ 治疗方案符合临床指南

#### 2. 完整性检查
- ✅ 24个字段全部填充
- ✅ 无空值
- ✅ 无None或undefined

#### 3. 格式规范性
- ✅ 日期格式统一: YYYY-MM-DD HH:MM
- ✅ 单位统一: ℃, mmHg, mmol/L
- ✅ 术语规范: 医学标准术语

#### 4. 逻辑一致性
- ✅ 主诉-诊断-治疗链条完整
- ✅ 化验结果与疾病相符
- ✅ 病程记录时间连续

---

## 8. 优势与局限

### ✅ 优势

1. **快速生成**: 100份病历 < 1秒
2. **高度可控**: 通过模板精确控制内容
3. **易于扩展**: 添加新疾病只需增加模板
4. **一致性强**: 规则引擎保证逻辑自洽
5. **无隐私问题**: 完全合成，零真实数据

### ⚠️ 局限

1. **模式固定**: 基于模板，可能缺乏真实数据的复杂性
2. **罕见情况**: 难以生成罕见病例和特殊情况
3. **文本多样性**: 受限于模板数量
4. **深度学习**: 不如GAN/GPT生成的自然语言流畅

### 🔄 改进方向

可结合深度学习提升质量:

```
模板生成系统 (当前)
    +
GPT文本优化 (未来)
    +
Synthcity表格数据生成
    ↓
更高质量的合成病历
```

---

## 9. 与机器学习方法对比

| 特性 | 模板生成 (本项目) | GAN/VAE生成 | GPT生成 |
|------|------------------|-------------|---------|
| **速度** | ⚡⚡⚡⚡⚡ 极快 | ⚡⚡⚡ 快 | ⚡⚡ 中等 |
| **可控性** | ⭐⭐⭐⭐⭐ 完全可控 | ⭐⭐ 弱 | ⭐⭐⭐ 中等 |
| **多样性** | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐⭐ 高 | ⭐⭐⭐⭐⭐ 极高 |
| **逻辑一致性** | ⭐⭐⭐⭐⭐ 完美 | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐ 好 |
| **需要训练数据** | ❌ 不需要 | ✅ 需要大量 | ✅ 需要 |
| **计算资源** | ⚡ 低 | 🔥🔥🔥 高 | 🔥🔥 中 |
| **易于调试** | ✅ 容易 | ❌ 困难 | ⚠️ 中等 |
| **适用场景** | 标准化病历 | 复杂数据 | 自然文本 |

---

## 10. 总结

### 🎯 核心原理

```
医疗病历数据生成 =
    模板库 (预定义的医学知识)
    +
    规则引擎 (疾病-症状-治疗关联)
    +
    随机化 (多层次随机选择和组合)
    +
    一致性保证 (逻辑链条验证)
```

### 📊 生成统计

- **模板总数**: 100+ 个
- **组合可能性**: 数百万种
- **生成速度**: 100份/秒
- **数据质量**: ⭐⭐⭐⭐⭐

### 💡 关键思想

这是一个**知识工程**方法，而非**机器学习**方法:
- 将医学专家知识编码为模板和规则
- 通过随机组合产生多样性
- 通过规则引擎保证一致性
- 适合快速生成标准化、高质量的医疗文档

---

**END**
